# image-gen-benchmark
An experimental test harness for benchmarking high-end text-to-image models under controlled conditions.  Generates multiple samples per prompt, enforces consistent inference settings, and enables structured comparison across realism, lighting, coherence, and prompt adherence metrics.
